# Results

All tables in this document are generated by `scripts/summarize.py` from the
stored result CSVs (`RESULTS_tables.md`); numbers here are copied from that
output rather than typed by hand.

**Verification.** Every quantitative claim below is independently recomputed
from the raw catalog and stored results by `scripts/factcheck.py`, which
asserts each value and fails loudly on any mismatch. Current status:
**93 checks, 0 failures.** External citations were verified against arXiv and
the publisher of record (see §8).

Setup: VarWISE Pure Catalog (457,080 objects), 28 catalog-derived features,
5 random seeds (3 for the XGBoost run), 30% stratified test split, pool capped
at 120,000, seeded with 2 examples/class, 60 rounds x 15 queries (14 → ~914
labels).

---

## 1. Headline: label efficiency

**Margin sampling reaches what random sampling achieves at 914 labels using
127 labels — an 86% label saving.** This is well past the 15–20% kill
threshold and holds on both estimators.

Track B (independent SIMBAD labels), XGBoost — VarWISE's own booster family:

| | macro F1 | labels |
|---|---|---|
| Full supervision | 0.935 ± 0.002 | 120,000 |
| **Margin sampling** | **0.913 ± 0.006** | **914** |
| Random sampling | 0.808 ± 0.002 | 914 |

Active learning reaches **97.7% of full-supervised performance with 0.76% of
the labels**. Random sampling at the same budget reaches 86.4%, and never
reaches even 90% of the full-supervised score within the tested budget.

Label savings to match random sampling's final score:

| strategy | LightGBM | XGBoost | Track A (distillation) |
|---|---|---|---|
| margin | **85.7%** | **86.1%** | 72.4% |
| class_balanced | 82.1% | 63.9% | 65.0% |
| prototype | 79.2% | 74.1% | 60.8% |
| uncertainty | 79.2% | 71.1% | 60.6% |
| quota | never matched | never matched | 27.6% |

---

## 2. The saving is concentrated in the rare classes

This is the specific finding, and it is consistent across estimators: the
benefit of active learning is almost entirely confined to the rarest classes.
Common classes are learned equally well by random sampling.

Track B, XGBoost, F1 at 914 labels:

| class | prevalence | random | best active | gap |
|---|---|---|---|---|
| `cv` | 0.14% | 0.609 | **0.881** | **+0.271** |
| `cep` | 0.92% | 0.438 | **0.817** | **+0.379** |
| `yso` | 5.44% | 0.862 | 0.914 | +0.052 |
| `rr` | 5.49% | 0.838 | 0.907 | +0.069 |
| `agn` | 8.45% | 0.962 | 0.981 | +0.019 |
| `lpv` | 29.10% | 0.972 | 0.983 | +0.011 |
| `ecl` | 50.47% | 0.973 | 0.984 | +0.011 |

Under LightGBM the same pattern is sharper still (`cv` +0.417, `cep` +0.394,
versus `ecl` +0.012).

### It is not just rebalancing

The obvious objection is that active learning simply builds a class-balanced
training set, and any balanced sample would do as well. Tested directly
(`scripts/diagnose_gap.py`) with a random draw carrying **identical per-class
counts** to the AL labeled set, so composition is held fixed and only the
choice of examples differs (5 seeds):

| class | prevalence | AL F1 | matched-random F1 | gain |
|---|---|---|---|---|
| `cv` | 0.14% | 0.861 | 0.701 | **+0.160 ± 0.036** |
| `cep` | 0.92% | 0.821 | 0.527 | **+0.294 ± 0.012** |
| `yso` | 5.44% | 0.913 | 0.864 | +0.049 ± 0.005 |
| `rr` | 5.49% | 0.864 | 0.819 | +0.045 ± 0.022 |
| `agn` | 8.45% | 0.980 | 0.963 | +0.017 ± 0.004 |
| `lpv` | 29.09% | 0.983 | 0.973 | +0.010 ± 0.001 |
| `ecl` | 50.47% | 0.980 | 0.963 | +0.017 ± 0.003 |

**Spearman ρ(prevalence, gain) = −0.857.** Rebalancing accounts for part of
the effect; informativeness accounts for the rest, and its contribution grows
as the class gets rarer.

Contamination was ruled out first: zero identical feature rows shared across
the pool/test split.

---

## 3. Correction: active learning does not beat full supervision

An earlier version of this analysis reported active learning *exceeding* the
120,000-label reference (macro-F1 0.91 vs 0.77). **That was an artifact of the
estimator.** LightGBM, at these hyperparameters, collapses on the rare classes
under the natural distribution — `cv` F1 = 0.230 with all 120,000 labels.
XGBoost does not: it reaches macro-F1 0.935 on the same data.

| reference (Track B, 120,000 labels) | macro F1 | `cv` F1 |
|---|---|---|
| LightGBM, natural distribution | 0.769 | 0.230 |
| LightGBM, `class_weight='balanced'` | 0.697 | 0.152 |
| LightGBM, balanced undersample (1,155) | 0.760 | 0.354 |
| **XGBoost, natural distribution** | **0.935** | — |

The correct claim is the ordinary one: active learning *approaches* full
supervision with ~130× fewer labels. The label-efficiency and rare-class
results above are unaffected, because they are within-estimator comparisons
and reproduce on both boosters.

---

## 4. Distillation targets understate the benefit

Track A trains on VarWISE's own `vartype` predictions; Track B on independent
SIMBAD types. Label savings are **72.4% on Track A versus 85.7% on Track B**
(same estimator).

The reason is that VarWISE's predicted class distribution is far more balanced
than reality — it over-predicts the rare classes:

| class | VarWISE predicted | SIMBAD truth |
|---|---|---|
| `cv` | 7.51% | 0.14% |
| `sn` | 2.10% | 0.016% |
| `cep` | 0.70% | 0.92% |

Because the imbalance is milder in the distillation target, there is less for
active learning to exploit. **A label-efficiency study run on a catalog's
prediction column will understate what active learning is worth on the real
task.** This is worth flagging given how commonly astronomical ML work trains
on catalog columns that are themselves upstream classifier output.

---

## 5. Negative result: hard per-class quotas fail

`quota` — which reserves a guaranteed share of every query batch for each
class — performs **worse than random sampling** on Track B under both
estimators (0.690 vs 0.781 LightGBM; 0.766 vs 0.808 XGBoost), and never
matches random's endpoint. This reproduces the observation recorded in
`chandra-toolkit` that hard quotas do not rescue severely rare classes: the
reserved slots are filled by the classifier's own top-P(class) candidates,
which under few labels are barely discriminative, so the quota spends budget
on uninformative examples.

`prototype`, the classifier-independent strategy, does not beat plain
`margin` here either, though it is competitive (74.1% vs 86.1% XGBoost).

Simple margin sampling is the recommendation.

---

## 6. Independent validation of VarWISE against SIMBAD

Scoring VarWISE's published `vartype` against 220,419 objects carrying an
independent SIMBAD type (`scripts/validate_varwise.py`):

| class | precision | recall | F1 | support |
|---|---|---|---|---|
| `ecl` | 0.995 | 0.984 | 0.989 | 111,241 |
| `rr` | 0.934 | 0.963 | 0.948 | 12,106 |
| `lpv` | 0.942 | 0.861 | 0.899 | 64,132 |
| `cep` | 0.801 | 0.924 | 0.858 | 2,020 |
| `agn` | 0.842 | 0.802 | 0.822 | 18,617 |
| `yso` | 0.939 | 0.342 | 0.501 | 11,967 |
| `cv` | 0.019 | 0.738 | 0.037 | 301 |
| `sn` | 0.002 | 0.229 | 0.005 | 35 |
| **macro avg** | 0.684 | 0.730 | **0.632** | 220,419 |
| **weighted avg** | 0.957 | 0.896 | 0.918 | 220,419 |

Dominant systematic confusions:

- **8,291 SIMBAD long-period variables classified `cv`** (the `cv` class is
  ~98% contaminated on this subset)
- **3,275 SIMBAD AGN classified `sn`**
- **YSOs scatter** into `agn` (19.7%), `cv` (21.2%), `lpv` (23.8%); only 34.2%
  recovered

**Caveats.** This is not like-for-like with the paper's reported macro-F1 of
0.95, which is measured against a held-out split of its own Gaia/ZTF-derived
training labels. SIMBAD covers 48% of the Pure Catalog and is biased toward
bright, well-studied objects; precision here is computed only over
SIMBAD-typed objects and is not catalog-wide precision. Some LPV/CV confusion
may be astrophysically genuine, since symbiotic and interacting systems are
labeled inconsistently across surveys. The common classes hold up well. The
size and consistent direction of the LPV→`cv` and AGN→`sn` confusions
nonetheless warrant caution when using VarWISE's rare-class labels.

---

## 7. How novel is this?

Classified honestly, finding by finding. This is an **application and
validation** study, not a new-method paper.

| # | Finding | Novelty class | Strength |
|---|---|---|---|
| 6 | VarWISE scores macro-F1 0.632 against independent SIMBAD labels | **New empirical finding about a specific published artifact.** No independent validation of VarWISE exists; the catalog is ~3 months old. | Strongest standalone result. Does not depend on active learning at all. |
| 4 | Distillation targets understate AL benefit (72.4% vs 85.7%) | **New methodological caution.** Not something I could find stated elsewhere. | Conceptually the most interesting, but n=1 archive — suggestive, not established. |
| 2 | Rare-class gain survives a composition-matched control (ρ = −0.857) | **Methodological refinement + new domain.** That AL helps under imbalance is known; separating *rebalancing* from *informativeness* with a count-matched control is rarely done. | Solid. The control is the part that adds rigour. |
| 1 | 86% label saving | **Replication in a new archive.** RB-C1000 reports ~60%; El-Kholy & Hayman report comparable gains. | Confirmatory. Expected direction, larger magnitude. |
| 5 | Hard per-class quotas underperform random | **Replication of a negative result** (same as `chandra-toolkit`). | Useful, low novelty. |

What this study is **not**: a new active-learning algorithm, a new acquisition
function, or evidence that active learning works in general — that was
established by Richards et al. (2011) for variable stars specifically, fifteen
years ago. The acquisition functions used here are all standard.

The honest one-line summary: *applying a known technique to a new archive,
with one useful control most such papers omit, plus an unplanned catalog
audit that may matter more than the headline result.*

## 8. Provenance of external claims

| Claim | Source | Verified |
|---|---|---|
| VarWISE, ApJS 284:41, Pure 457,080 / Extended 1,918,082 objects | Paz, Kirkpatrick, Uttamchandani, Raen & Cutri 2026, [arXiv:2605.19059](https://arxiv.org/abs/2605.19059) | Abstract, verbatim |
| VarWISE macro-F1 0.95; 910,697 training / 113,840 validation | same, full text | Verbatim: *"resulting in a macro-averaged F-1 score of 0.95"* |
| VarWISE uses VARnet + XGBoost, Gaia + ZTF training labels | same | Abstract + text |
| RB-C1000, A&A 693 A105 (2025), ~1000 labels beats 2500-label supervised | Liu et al., [arXiv:2412.02409](https://arxiv.org/abs/2412.02409) | Confirmed |
| AL for habitability under extreme imbalance, PASP 138(5) 2026 | El-Kholy & Hayman, [arXiv:2602.23666](https://arxiv.org/abs/2602.23666) | Confirmed; margin sampling vs random, gradient-boosted baseline |
| AL for photometric variable-star classification (prior art) | Richards et al. 2011, [arXiv:1106.2832](https://arxiv.org/abs/1106.2832) | Confirmed |
| No prior AL work on VarWISE or NEOWISE | Searches for "VarWISE + active learning", "NEOWISE + active learning classification" (Aug 2026) | No hits |

Catalog data: IRSA TAP tables `varwisepure` / `varwiseext`,
DOI [10.26131/IRSA656](https://doi.org/10.26131/IRSA656). Downloaded row count
(457,080) matches the published Pure Catalog count exactly.

## 9. Limitations

- **Features are an approximation.** VarWISE's 31 classifier features derive
  from raw NEOWISE light curves (Fourier coefficients, Stetson indices, χ²
  statistics) held in the separate Associations table, not the published
  catalog. This study uses the 28 reproducible from catalog columns. That the
  full-supervised weighted F1 (0.979 XGBoost, 0.947 LightGBM) sits close to
  VarWISE's reported 0.95 suggests the approximation is adequate, but it is
  not a reproduction.
- **`sn` was excluded** from Track B: 35 SIMBAD-confirmed objects, F1 = 0.000
  even against the full reference. Not learnable from catalog features at this
  sample size.
- **`ea`/`ew` merge** in Track B because SIMBAD does not separate Algol from
  W UMa eclipsing binaries.
- **Prior art.** Richards et al. 2011 already applied active learning to
  photometric variable-star classification. The contribution here is the
  archive (NEOWISE/VarWISE), the per-class rare-class quantification against a
  composition-matched control, and the distillation-vs-real-labels contrast.
- **XGBoost run used 3 seeds** rather than 5, for runtime.
- Pool capped at 120,000 of ~154,000 available Track B training rows.
